# Relève en local : http://localhost:8081
#
#   docker compose -f releve/docker-compose.local.yml up --build

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:80"
    restart: unless-stopped
